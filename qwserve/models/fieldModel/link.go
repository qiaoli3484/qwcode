package fieldModel

type Lextend struct {
	Text string
}

/*
func (t *Lextend) renderDetail(data, talias, alias string) string {
	link := fmt.Sprintf("layer_show('%s','/link?tid=%d&fname=%s&rid='+rid)", field.ItemName, field.LinkTo.AcTemplet, field.Alias)
	str = element.Link(val).Primary().Click(link).View()
}
*/
